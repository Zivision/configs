#!/usr/bin/env bash

PROJECT_DIR=$(dirname "$(dirname "$(readlink -f "$0")")")

echo "Rebuilding System"
# Saving this for future reference, but rebuild will need a refacture
# sudo nixos-rebuild switch --flake "$PROJECT_DIR/nixos#swiftx" --impure
sudo nixos-rebuild switch --flake "$PROJECT_DIR/nixos" --impure
#sudo nixos-rebuild test --flake .#desktop
echo "Finished!"
