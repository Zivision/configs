#!/usr/bin/env bash

export CONFIGS=$(cat $(fd -I paths.json ~) | jq -r ".root")
export DOOMDIR="$CONFIGS/build/doom"

waypaper --restore
emacs -q --load "$CONFIGS/build/emacs/init.el" --daemon
foot --server
